analyzer:
  exclude:
    # Pinned to zenrouter 2.2.0 / zenrouter_core 2.1.0. Root analyze would
    # otherwise type-check it against the current workspace API.
    - benchmark/zenrouter/old/**
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
